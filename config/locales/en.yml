en:
  hello: "Hello world"
  errors:
    format: "%{attribute} %{message}"
  activerecord:
    errors:
      models:
        tracker:
          attributes:
            amount:
              greater_than_left_amount: "must be less than or equal to the available dry food amount, which is %{left_amount}."