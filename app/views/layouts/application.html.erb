<!DOCTYPE html>
<html class="scroll-smooth" lang="<%= I18n.locale %>">
  <head>
    <title><%= content_for(:title) || "Aji Tracker" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Rubik:ital,wght@0,300..900;1,300..900&display=swap" rel="stylesheet">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <%= favicon_link_tag 'aji_favicon_32.png' %>

    <script type="module">
      import Big from "https://ga.jspm.io/npm:big.js@6.2.1/big.mjs";
      window.BigNumber = Big;
    </script>

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag "tailwind", "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <%= javascript_include_tag "https://www.gstatic.com/charts/loader.js" %>
  </head>

  <body class="dark:bg-darkBlue dark:text-white overflow-x-hidden">
    <%= render 'shared/gemini_key_banner' %>
    <%= render 'layouts/navigation' %>
    <%= render 'layouts/messages' %>
      <%= yield %>
    <div>
      <%= render 'layouts/footer' %>
    </div>
  </body>
</html>